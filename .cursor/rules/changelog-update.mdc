---
description: 有用户可见能力变化时，在 CHANGELOG.md 文首追加条目（与提交草稿配套）
alwaysApply: true
---

# 任务结束更新 CHANGELOG

在本仓库完成一轮**有用户可见能力变化**的任务后（即将回复用户前），必须更新根目录 `CHANGELOG.md`。与 `.cursor/rules/git-commit-draft.mdc` **配套执行**（该写草稿时通常也该写 CHANGELOG）。

## 何时执行

- 本轮新增 / 调整了**使用者能感知**的能力（编辑体验、词条提示、侧栏、备注、快捷键等）→ **写入**
- 纯问答、未改仓库 → **跳过**
- 仅内部重构、修类型、改注释、无行为变化 → **跳过**（可只写提交草稿）
- 仅改颜色/间距/文案等极小样式且用户无感 → **跳过**
- 用户明确说「不要改 CHANGELOG」→ **跳过**
- 用户明确要求「更新 CHANGELOG / 记一笔发版」→ **即使小改也写**

## 怎么写

1. 打开 `CHANGELOG.md`，在文首说明段落之后、**第一条 `##` 版本节之前**插入新节（越新越靠上）。
2. 标题格式：`## YYYY-MM · <主题短名>`  
   - 月份用本轮完成时的本地年月（如 `2026-08`）  
   - 同一月可有多节；主题要能让使用者扫一眼看懂
3. 正文用 `-` 列表，写**使用侧能力**，不写文件名、类名、插件名等实现细节。
4. 要点用 **加粗短标签** + 一句说明（对齐现有条目文风）。
5. 若改动了用户向语法 / 虚线含义等，必要时同步改文末「语法速查」表。
6. **不要**为此去改 `package.json` 的 `version`，除非用户明确要求升版本号。

## 示例结构

```markdown
## 2026-08 · 词条跟打提示

- **前缀多选气泡**：输入「暴击」等公共前缀且有多条更长词条时，停手后弹出候选。
- **Tab 再选词**：气泡先不抢键盘；按 Tab 进入选词后再用数字键 / 方向键确认。
- **幽灵补全**：仅唯一更长词条时，光标后显示灰色后缀，Tab 一键补全为词条引用。
```

## 与提交草稿的关系

| 产物 | 路径 | 面向 |
|------|------|------|
| 提交草稿 | `.cursor/commit-draft.md`（gitignore） | 开发者复制 commit |
| 功能清单 | `CHANGELOG.md`（入库） | 使用者看能力变化 |

- 两者都写时：先改 `CHANGELOG.md`，再写提交草稿，草稿「建议提交说明」里可带一句「更新 CHANGELOG」。
- 回复用户时提一句：`CHANGELOG.md` 已追加对应条目。

## 注意

- **只追加文档，不要自动 `git commit` / `git push`**（除非用户明确要求提交）
- 不要把内部 bug 排查过程写进 CHANGELOG；写成修好后的能力描述即可
