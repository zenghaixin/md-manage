---
description: 每次任务完成后把 git 改动摘要写入 .cursor/commit-draft.md，方便复制提交
alwaysApply: true
---

# 任务结束写 Git 提交草稿

在本仓库完成一轮**有代码/配置改动**的任务后（即将回复用户前），必须生成可直接复制的提交说明文件。

若本轮还有**用户可见能力变化**，须同时按 `.cursor/rules/changelog-update.mdc` 更新 `CHANGELOG.md`（先 CHANGELOG，再写本草稿；建议提交说明可带「更新 CHANGELOG」）。

## 何时执行

- 本轮有**较完整**的功能 / 重构 / 多文件改动，且任务已收尾 → **写入**
- 纯问答、未改仓库 → **跳过**
- 用户明确说「不要写提交草稿」→ **跳过**
- **小改动跳过**（不写、不更新 `.cursor/commit-draft.md`），例如：
  - 仅改颜色、间距、文案、单处样式
  - 只动 1 个文件且改动很少（约十几行以内）
  - 打日志、改注释、改配置开关等琐碎调整
- 用户明确要求「写提交草稿 / 提炼 commit」→ **即使小改也写**

## 怎么做

1. 在仓库根目录执行（PowerShell 用 `;` 分隔）：
   - `git status`
   - `git diff --stat HEAD`
   - `git log -5 --oneline`（对齐近期文风）
2. **覆盖写入** `.cursor/commit-draft.md`（路径固定，不要每次新建时间戳文件）
3. 回复用户时用一两句话提到：提交草稿已写入 `.cursor/commit-draft.md`

## 文件格式（必须）

改动项一律用 **`1、` `2、` `3、`** 编号（中文顿号），不要用 `-` 列表。

```markdown
# Git 提交草稿

> 生成时间：YYYY-MM-DD HH:mm（本地）
> 分支：<branch>
> 相对提交：<short-hash> <subject>

## 汇总标题

（单行，复制到 GitHub Desktop 的 **Summary**，或 `git commit -m`）

\`\`\`
<一句概括本轮主线的标题，约 20～50 字>
\`\`\`

## 建议提交说明

（复制到 GitHub Desktop 的 **Description**，或 `git commit` 正文）

\`\`\`
1、<改动要点一>
2、<改动要点二>
3、<改动要点三>
\`\`\`

## 改动摘要

1、<要点 1>
2、<要点 2>

## 文件一览

\`\`\`
<粘贴 git diff --stat 或精简后的路径列表>
\`\`\`
```

### 汇总标题怎么写

- **必填**：每份草稿都要有「汇总标题」，单独一节，便于一眼复制。
- **一行**：不用 `1、` 编号；不用句号结尾；避免换行。
- **概括主线**：写「这轮最主要做了什么」，不要堆文件名；多主题用顿号或「与」连接。
- **长度**：约 20～50 字；GitHub Desktop Summary 有长度限制，过长会被截断。
- **与正文分工**：标题 = 总览；「建议提交说明」= 分条细节（可 3～6 条）。

示例：

\`\`\`
词条系统重构：浮层/备注修复与 draggable-float、key-picker 组件整理
\`\`\`

## 注意

- **只写草稿，不要自动 `git commit` / `git push`**（除非用户明确要求提交）
- 草稿文件已在 `.gitignore`，勿强制加入版本库
- **GitHub Desktop**：`汇总标题` → Summary；`建议提交说明` → Description
- **命令行**：`git commit -m "汇总标题" -m "建议提交说明全文"`，或仅 `-m` 标题、正文另附
- 「建议提交说明」风格对齐近几次 `git log`（`1、… 2、…`）
