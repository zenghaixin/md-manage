---
description: 后续再做、暂缓、放弃的功能写入 BACKLOG.md；做完或不做时更新条目
alwaysApply: true
---

# 待办功能 BACKLOG

根目录 `BACKLOG.md` 记录**已对齐方向、尚未实现**的能力。与 `CHANGELOG.md` 分工：

| 文件 | 时机 | 内容 |
|------|------|------|
| `BACKLOG.md` | 以后再做 / 暂缓 | 计划中的能力 |
| `CHANGELOG.md` | 本轮已交付 | 使用者已能感知的变化 |

## 何时写入 BACKLOG

- 与用户讨论后明确 **「以后再做 / 下一期 / 暂缓 / 先记待办」** → 在 `BACKLOG.md` 对应章节追加 `- [ ]` 条目
- 纯随口想法、未达成共识 → **不写**
- 本轮正在实现的功能 → **不写**（做完写 CHANGELOG）

条目写**用户可感知的能力**，少写文件名、类名。

## 何时更新 / 删除条目

- **已实现**：从 `BACKLOG.md` **删除**该条（或改为 `[x]` 后下一轮清理）；用户向能力写入 `CHANGELOG.md`
- **明确不做 / 方案废弃**：整条 **删除**，或保留并 **加删除线**：`~~条目说明~~`
- **范围缩小 / 合并**：改 wording 或合并为一条，删掉重复

## 格式

- 待做：`- [ ] **短标签**：一句说明`
- 已完成（临时标记，优先删）：`- [x] **短标签**：…`
- 放弃：`- ~~**短标签**：…~~` 或直接删

按现有章节归类（词条 · 右栏、壳层 · 右栏等）；无合适章节则放在「设置与其它」。

## 注意

- **只改 `BACKLOG.md`，不要自动 commit**
- 用户明确说「不要记待办」→ 跳过
- 回复用户时可一句带过：`已记入 BACKLOG.md`（若本轮有写入）
