# Role
你是本仓库（md-manage）的协作开发助手：Vue 3 + TipTap Markdown 文档管理器。
多人并行开发时，优先保证核心壳稳定，新功能以扩展方式接入。

# Coding Principles
1. 优先简洁、可读、类型安全。
2. 优先边界与异常。
3. 避免无谓计算；Vue 中合理使用 computed / watch。
4. 关键算法用简明中文注释。
5. 新功能经扩展钩子与壳事件接入，不硬改 MarkdownEditor / DocsPage / main。

# Architecture：壳 vs 扩展（必读）

**详细边界见 Cursor 规则：**
- 通用 UI / 壳组件 → `.cursor/rules/shared-components.mdc`
- Markdown 扩展 → `.cursor/rules/markdown-extensions.mdc`

## 核心壳（尽量少改）
- `src/components/MarkdownEditor.vue`
- `src/pages/DocsPage.vue`
- `src/main.js`
- `src/editor/shellEvents.ts`（壳事件总线：打开文件、保存、右栏开关等）
- `src/editor/rightPanelRegistry.ts`（右侧书签模块注册）
- `src/components/OpsSidePanel.vue` / `FileSidebar.vue` 等布局壳

壳只提供：布局、文件树、编辑器挂载、右栏宿主、通用事件。
**禁止**在壳里 `import` 具体业务扩展实现（如 `term-glossary/*`、`remark/*`、未来的 AI/图谱模块）。

## 自定义 / 业务功能写哪里
- **Markdown 自定义语法、编辑态行为、落盘变换** → `src/editor/extensions/<extension-id>/`
  - 入口：`index.ts` 导出符合 `MarkdownExtension` 的对象
  - 在 `src/editor/extensions/index.ts` 里 `registerExtension(...)` 注册
  - 参考：`term-glossary/`、`remark/`
- **右侧功能区面板** → 扩展内实现 UI，经 `rightPanelRegistry` 注册书签；需要打开右栏时用 `shellEvents.requestOpenRightPanel`
- **跨组件协作**（打开某文件、强制保存、定位标题等）→ 优先扩 `shellEvents`，由壳注册监听；不要把扩展 import 进 DocsPage
- **可复用 UI**（拖拽弹窗、数字气泡、选区气泡等）→ `src/components/<name>/`，经 `index.ts` 导出；扩展只传内容与回调
- **应用级偏好**（主题、右栏上次模块）→ `.app-config.json` + `useAppConfig`；**不是** glossary 数据

## 何时允许改壳
仅当扩展接口不够用时，做**最小通用能力**补丁，例如：
- 在 `shellEvents` 增加一类与业务无关的事件
- 在 `rightPanelRegistry` / hooks 增加通用挂载点
- 修壳本身的 bug / 布局

业务逻辑、词条规则、备注逻辑、AI 分析、关系图等：**写在 extensions（或独立 feature 目录）**，不堆进 MarkdownEditor / DocsPage。

## 数据文件约定
- 词库镜像：`md/词条/.glossary/`（侧栏不展示；按下标命名，如 `0.json` / `0_0.json`）+ `index.json`（key↔path）
- 每个入口 md 对应一个 `{key}.json`：必有 `list` + `terms`（无 type/attrs）；API 仍聚合为 `{ lastUpdated, terms }`
- 扩展落库语法与字段约定写在扩展目录与下文

# Constraints
- 除非特别要求，不写无关 Markdown 解释。
- 禁止 eval 与高危写法。
- 驼峰命名；Vue 优先 `<script setup>`。
- 禁止在 MarkdownEditor / DocsPage / main 中 import 具体扩展实现。
- 新增扩展不要绕过 `registerExtension` 私自挂到编辑器。

# Built-in Extension: term-glossary（词条）

## 目录
- `core/`：共享引擎，按职责分子目录：
  - `shared/`：constants、styles、editorViewRef、flashTerm、glossaryPaths
  - `model/`：syntax、node、termRef、termOps、nestGuard、serializeDesc、TermNodeView
  - `match/`：segmenter、match、convert、highlight
  - `plugins/`：interaction 装配 + autoConfirm / click / prompt
  - `dialog/`：词条弹窗
  - `panel/`：编辑浮层、右栏词条、不是词条
  - `rename/`：改名与冲突抽屉
- 根 `index.ts`：组装注册
- 运行时数据在 `md/词条/.glossary/`；**分类靠文件夹，无 type/attrs**

## 落库与目录（当前定案）
- **定义块**只认 `md/词条/` 下 `::: term`；默认新建落盘 `词条/默认词条.md`
- 其它路径的 `::: term` 原文显示、不解析、不入库；引用 `term[标题]` 仍可在任意 md
- 系统文件夹「词条」：置顶、不可删/改名/调序
- 分类用「词条」下文件夹（及入口 md）表达；已废弃 basic/特殊类型体系

## 落库语法
### 定义（有空格）
```md
::: term [标题]
描述
:::
```

### 已确认引用（无两侧空格）
```md
角色term[HP]归零
```
- 编辑区显示高亮标题；`×` 取消确认 → 裸文本，并写入短 ignoreContexts，避免立刻再自动确认
- 手写无效 `term[不存在]`：红虚线，可 `×` 删除

## 识别（segmentit）
- 全量 segmentit + glossary 标题作自定义词库（`词|0x1000|999999`）
- 防抖约 400ms；IME composition 期间不转换
- **整词命中且在词表** → 自动写成 `term[标题]`
- **切不出整词但子串像词条** → 灰线，用户点选确认
- **不再提供**空格/`%%` 等包一层快捷输入
- 标题经 `sanitizeTermTitle` 清洗；空标题不进词库
- 定义块不完整（无标题或空描述）且光标不在块内 → 整块标红提示

## glossary 落库（md/词条/.glossary）
- 文件名按下标镜像侧栏树序：`词条/默认词条.md` → `0.json`；有默认词条时 `词条/世界观/历史事件.md` → `1_0.json`
- `index.json`：`{ version, entries: [{ key, path }] }`；树增删/调序时重建并改名 json
- 每个 `{key}.json` 形如：
```json
{
  "list": ["测试1"],
  "terms": {
    "测试1": {
      "title": "测试1",
      "description": "...",
      "sourcePath": "词条/默认词条.md",
      "ignoreContexts": [],
      "formerTitles": [],
      "pendingManualConfirm": []
    }
  }
}
```
- `list`：本文件词条名称索引（必有）；与 `terms` 的 key 对齐
- `terms`：完整词条详情（无 `type`/`attrs`）；API 聚合为单一 `terms` 表给前端
- 插入：`Mod-Alt-t`（全局）→ 新建浮层（可选「存储位置」）→ 确认后追加到所选 md，不插当前文引用、不跳转；缺默认文件则重建
- 定义块只读展示标题 + 描述；选中后右上角「编辑 / 删除」
- 改名：旧名进 `formerTitles`；已确认 `term[旧]` 全库改为 `term[新]`
- 曾用名仅提示不自动改；新名撞正文 → 右侧冲突抽屉
- `pendingManualConfirm`：改名扫描写入的待人工确认列表；非空时该标题不自动确认

## 关键文件
- `core/`：语法、匹配、改名、确认等共享引擎
- `md/词条/.glossary/`：下标镜像词库 + `index.json`
- `stores/glossary.ts`、`server/glossaryStore.js`（读写聚合）

## API
- GET/PUT `/api/glossary`
- GET `/api/glossary/entries`（入口 md 列表，供新建下拉）
- POST `/api/glossary/sync`
- PATCH `/api/glossary/file`
- POST `/api/glossary/rename-sync`
- POST `/api/glossary/apply-conflicts`

# Built-in Extension: remark（正文备注）
- 目录：`src/editor/extensions/remark/`
- 右栏备注面板经 `rightPanelRegistry` 挂载；细节以该目录代码为准

# 规划中能力（见 `待处理.md`，未实现勿当已有行为）
- 关系图、事件总览、AI 只读分析等
- 实现时仍按「扩展 + 右栏模块 + shellEvents」接入，不改壳业务化
