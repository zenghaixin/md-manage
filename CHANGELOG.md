# 功能更新清单

按使用侧能力整理（非实现细节）。后续每次发版可在文首追加新版本。

---

## 2026-08 · 词条跟打提示与幽灵补全

- **前缀多选气泡**：输入「暴击」等公共前缀，且词库有多条更长标题（如暴击率 / 暴击伤害）时，停手后弹出候选列表（不必先有短词条「暴击」）。
- **Tab 再选词**：气泡弹出不抢焦点，可继续打字；先按 **Tab** 进入选词，再用数字键或上下键选择；Esc 关闭。
- **幽灵补全**：仅匹配到**唯一**更长词条时，光标后显示灰色后缀（如输入「魔剑」显示「-卡特托里」）；**Tab** 一键补全为已确认 `term[]`；继续输入与完整标题不一致时自动取消。
- **多候选不显示幽灵**：有多条候选时只出气泡，不出灰色后缀，避免干扰。
- **浮层贴边**：新建词条等浮层在靠近屏幕底部时会上移，避免底部被裁切。

---

## 2026-08 · 选区设置词条与胶囊拖入

- **选区「设置词条」**：选中正文后，气泡在「备注」旁增加「设置词条」；打开新建词条浮层，标题预填为选中文案；确认后该处变为已确认引用。正在编辑词条入口文件时，只在当前文档追加定义并保存，不会用磁盘旧稿覆盖正文。
- **右侧胶囊拖入正文**：词条面板胶囊短按仍打开定义来源；**长按**（或按下后拖动）再拖到正文松手，在落点插入对应 `term[]` 引用。
- **词条之间的空行**：退格 / Delete 会先删掉空行，不再误选中相邻词条；光标贴在有正文的段落边界时，仍会选中整块词条以免误并进去。

---

## 2026-07 · 左侧大纲

- **侧栏书签 Tabs**：文件树上方用书签样式切换「文件 / 大纲」。
- **文档大纲**：展示当前打开文档的标题（H1–H6），按层级缩进；编辑或切源码时会同步更新。
- **点击跳转**：点大纲项可滚动定位到正文对应标题（编辑态 / 源码态均支持）。

---

## 2026-07 · 正文备注

- **加备注**：选中正文后，气泡菜单选「备注」，把选中文字标成备注。
- **正文样式**：备注默认淡黄字 + 下划线；光标在备注内、或悬停右侧对应卡片时，改为正文色并带淡黄底，便于区分。
- **右侧备注栏**：有备注时才出现「备注」书签；卡片与正文位置大致对齐，悬停卡片会高亮正文，光标在某段备注内时对应卡片边框高亮。
- **写描述**：在右侧卡片里填写备注说明；删除只在卡片上操作。
- **滚动联动**：编辑区与备注栏滚动同步；两侧滚动条隐藏，仍可用滚轮滚动。
- **落盘**：正文写作 `remark(id)[原文]`；描述保存在文件末尾隐藏块中，不影响阅读正文。

---

## 2026-07 · 侧栏文件夹管理增强

- **重命名文件夹**：文件夹行上，「新建文件」前新增铅笔图标；点击后可直接改文件夹名称并保存。
- **改名后路径同步**：文件夹改名后，相关词条记录的来源路径会一并更新，避免词条来源丢失。

---

## 2026-07 · 曾用名提示（正文里的旧标题）

- **琥珀色虚线提示**：正文中若出现某词条的「曾用名」（整词），会用琥珀色虚线标出，便于发现旧叫法。
- **点选处理**：
  - **切换为…**：把该处改成对应正式词条引用；若多个词条共用同一曾用名，可从列表中选择。
  - **本次不是**：仅忽略这一处，不再当成该曾用名提示。
  - **不再提示**：从相关词条中去掉该曾用名，以后全局不再提示。
- **新建 / 改名撞曾用名**：新标题若与其他词条的曾用名冲突，会弹窗提醒，避免误占旧名。

---

## 2026-07 · 词条改名与冲突处理

- **改名同步全文**：修改词条标题后，文档里已确认的 `term[旧标题]` 会批量改为新标题。
- **曾用名保留**：旧标题进入「曾用名」，方便正文继续识别历史叫法。
- **冲突抽屉**：改名后若正文仍有需人工确认的命中，右侧抽屉列出上下文；可勾选后批量确认或忽略。
- **忽略策略**：忽略会记住上下文，同一处不再反复弹出；改名不会清掉已有忽略记录。
- **暂停自动确认**：有待处理冲突时，该标题在正文中只显示灰色候选，不会自动包成 `term[]`，处理完后恢复。

---

## 2026-07 · 正文词条识别与引用

- **定义块**：用 `::: term [标题]` … `:::` 写词条定义；编辑时标题加粗高亮，描述按普通正文编辑。
- **引用语法**：确认后的引用写作 `term[标题]`（`term[` 两侧不留空格）。
- **自动 / 候选匹配**：
  - 整词命中词库标题 → 可自动确认成 `term[]`。
  - 仅子串命中 → 灰色虚线候选，需手动确认。
- **候选操作**：可确认成引用，或点 `×` 取消（恢复为普通文字，并短暂忽略该处）。
- **非法引用**：格式不对的 `term[]` 用红色虚线标出，便于发现写错。
- **输入法友好**：中文输入过程中不会抢先自动确认，避免半成品被包进引用。

---

## 2026-07 · 词条块编辑体验（TipTap）

- 编辑器改为 TipTap，词条块按「真实节点」编辑，光标 / 换行更接近普通文档。
- **标题与描述分区**：标题加粗；标题与描述默认换行；删光描述不会拆坏词条结构。
- **描述内换行**：描述中间可正常回车换行。
- **退出描述**：在描述末尾连续两次回车，退出词条块，回到普通正文编辑。
- **结构保护**：在词条外删除时，不易误删词条边界导致整块错乱。
- 落盘仍为 Markdown 自定义语法，打开即可继续编辑。

---

## 2026-07 · 文档侧栏与基础能力（既有 + 延续）

- **文件夹（标签）**：新建 / 删除文件夹；文件夹对应 `md/` 下目录。
- **文件**：在文件夹下新建 / 删除 `.md`；双击或长按可重命名文件。
- **编辑与自动保存**：打开文件后编辑，定时自动写入对应 Markdown。
- **移动端**：侧栏抽屉可用；文件可用长按触发重命名。

---

## 语法速查（用户向）

| 用途 | 写法 |
|------|------|
| 定义词条 | `::: term [标题]` + 描述 + `:::` |
| 已确认引用 | `term[标题]` |
| 正文备注 | `remark(id)[原文]`（描述在文末隐藏块） |
| 灰色虚线 | 待确认候选（点选确认或忽略） |
| 琥珀色虚线 | 曾用名命中（切换 / 本次不是 / 不再提示） |
| 红色虚线 | 非法或不完整的 `term[]` |
| 淡黄字 + 下划线 | 备注原文（编辑 / 悬停卡片时变正文色 + 淡黄底） |
