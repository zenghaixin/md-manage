# 待办功能（Backlog）

> 记录**已对齐方向、尚未实现**的能力。维护规则见 `.cursor/rules/backlog-update.mdc`。
>
> - **以后再做** → 在此追加 `- [ ]`
> - **做完了** → 删除该条，能力写进 `CHANGELOG.md`
> - **不做了** → 删除，或保留并加删除线 `~~…~~`

---

## 词条 · 右栏

- [ ] **浏览 + 预览**：全库浏览子页增加右侧词条预览（描述 + 引用 + 操作）
- [ ] **关系网等功能落地**：Hub「功能」区占位项
- [ ] **常用词条 · 固定收藏**：除「最近打开」外，支持 pin

## 词条 · 数据与字段

> **分类模型**：不再使用「词条类型」枚举；归类由「词条」下**文件夹**体现，字段模板挂在**入口文件**（如 `词条/角色/人物.md`）上。

- [x] **文件级 schema（首版）**：右栏「通用字段」书签 + 编辑填值 + 本条额外字段 + 预览展示
- [x] **右栏拆分**：词条汇总 / 通用字段 分书签；Hub 不再嵌字段模板
- [x] **新建入口文件默认 schema**：词条目录下新建 md 自动写入「标题 + 备注」，并打开右侧通用字段
- [ ] **引用槽位全面迁入 schema**：人物入口等仍在 `termRefSlots.ts` 按**文件路径**写死默认引用，与该入口 schema 的 term 字段打通后可去掉硬编码
- [x] **schema 字段排序**：通用字段设置里长按拖动调整顺序
- [ ] **schema 可视化增强**：枚举类型、必填标记等
- ~~**按词条类型驱动表单 / 属性**：已废弃，改由文件夹 + 入口文件名 + 文件级 schema~~
- ~~**新建入口文件时选模板（按类型）**：已改为默认「标题 + 备注」~~

## 词条 · 其它

- [ ] **汇总卡片**：按槽位分区展示引用关系

## 壳层 · 右栏

- [x] **按书签默认宽度**：模块注册 `defaultWidth`，切换书签时应用（词条汇总 / 通用字段 / 备注 520px，冲突审查 400px）
- [ ] **用户自定义各书签默认宽度**：`appConfig` 持久化，优先于模块 `defaultWidth`
- [ ] **各书签记住手动拖拽宽度**：切换回来恢复上次拖拽值（可选，与默认宽度策略二选一或分层）

## 设置与其它

- [ ] （在此追加跨模块待办）

---

## 说明

| 标记 | 含义 |
|------|------|
| `[ ]` | 待做 |
| `[x]` | 已完成（优先删除；或暂留作记录） |
| `~~删除线~~` | 已决定不做 / 方案废弃 |

新增条目时尽量写**用户可感知的能力**，少写实现细节；实现细节放 issue 或 PR 描述即可。
