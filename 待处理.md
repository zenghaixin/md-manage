# 待处理 · 词条扩展与后续能力

按依赖与价值排序。勾选表示已完成；未勾选项为规划中。

---

## 结论（先看这）

**必须先做（阻塞后续一切）：词条 `type` + 图标展示 + 编辑可选类型。**

没有稳定的类型字段，关系图 / 事件总览 / 人物卡 / AI「类型建议」都会缺少统一数据模型。  
右侧功能区已预留，适合挂后续模块，但**不要**在类型落地前并行大做 AI 或图谱。

---

## P0 · 关键基础（建议本期）

| 项 | 说明 | 为何关键 |
|----|------|----------|
| ✅ 词条 `type` 枚举 | 固定类型：普通、角色、阵营、职业、技能、地理、历史事件 | 后续模块的共同主键维度 |
| ✅ 默认与兼容 | 旧词条无 `type` → 视为「普通」；读写时规范化写入 | 不打断现有文档 / glossary |
| ✅ 快捷键选类型 | `Ctrl+Alt+T` → 气泡；1–6 特殊 / Esc·Enter 普通 / 点外侧取消 | 快速创建 |
| 编辑可选类型 | 定义块或词条弹层里可改 type | 没有入口等于字段无用 |
| 图标展示（1～2 处） | 建议：定义块标题旁已有文字标签；可再加图标 / 正文 `term[]` | 用户感知类型 |
| 同名策略 | 建议全局 `title` 仍唯一（跨类型也不重名），实现简单 | 避免匹配 / 改名逻辑爆炸 |

**落库建议（二选一，先定再写）：**

1. **✅ 已采用**：`type` 只存 `glossary.json`（代码里定义枚举与默认值），Markdown 语法暂不改。  
2. 可选：语法带类型，如 `::: term [标题] {type: character}`——迁移与解析成本更高。

**不靠提交 `glossary.json`：** 新字段由 store / server `normalize` 补默认；文件仍 gitignore。

---

## P1 · 右栏消费（类型落地后）

| 项 | 说明 | 依赖 |
|----|------|------|
| 按类型筛选 / 分组 | 右栏小模块或列表：只看角色 / 技能等 | P0 |
| 人物汇总卡片 | 聚合某「角色」的描述与引用处 | P0 + 右栏模块 |
| 历史事件总览 | 事件类型列表；后续再加时间字段 | P0；时间字段可 P1.5 |
| 人物关系图 | **另存关系边**（from/to/relation），勿塞进 description | P0 + 边数据模型 |

右栏书签继续走现有 `rightPanelRegistry`；`.app-config.json` 的 `rightPanelActiveModuleId` 只记上次打开的模块。

---

## P2 · AI 文档分析（助手层，非真相源）

| 项 | 说明 | 注意 |
|----|------|------|
| 提示词文件 | 如 `prompts/doc-analyze.md`：只分析、出结构化候选、不改文件 | 可版本管理、可提交 |
| 分析 API | 入：当前文件/选区；出：JSON（类型建议、关系/事件草稿） | AI **无写盘权限** |
| 右栏「AI 分析」 | 展示候选，用户勾选后再写入 glossary / 关系表 | 与冲突抽屉同一交互哲学 |
| 密钥与配置 | 本地配置，不进仓库 | 可插拔模型 |

**单靠提示词不够**：程序侧禁止 AI 直接改 Markdown / `glossary.json`；校验 schema 后再给 UI。

**AI 第一期建议只做一件：** 「类型建议」或「关系草稿」二选一，不要并行。

---

## P3 · 可后续再议

- 各类型专用语法（`::: character` 等）——目前不推荐，成本高  
- 类型相关主题色 / 更重视觉  
- 世界地图与地理钉点  
- 多模型、批量全库 AI 扫描  

---

## 建议实现顺序

```
1. type 枚举 + normalize + UI 选择 + 图标（P0）
2. 右栏：按类型列表 / 人物卡（P1 轻量）
3. 关系边模型 + 关系图 或 事件+时间（P1 选一）
4. prompts + AI 只读分析 + 右栏确认应用（P2）
```

---

## 与现有能力的关系（备忘）

| 现有 | 后续怎么用 |
|------|------------|
| `::: term` / `term[]` / 改名 / 冲突抽屉 | 继续作为正文与词库主链路（`core/`）；type 挂在词条上 |
| `types/` | 特殊词条仅样式/展示；自定义类型不进核心功能 |
| 右侧功能区 | 图谱、总览、AI、汇总卡的宿主 |
| `shellEvents` | 扩展打开右栏、定位正文，避免硬耦 DocsPage |
| `glossary.json`（gitignore） | 运行时数据；新字段靠代码默认与同步，不靠 git 传数据 |
| `.app-config.json` | 应用级偏好（主题、右栏模块等），与词条数据分离 |

---

## 待你拍板（开工前）

1. `type` 是否只进 glossary，第一期改不改 Markdown 语法？  
2. 图标第一期显示几处？  
3. P1 先做「人物卡」还是「关系边」？  
4. AI 是否排进下一季度，还是 P0 完成后再说？
