# 待处理 · 词条扩展与后续能力

按依赖与价值排序。勾选表示已完成；未勾选项为规划中。

---

## 结论（先看这）

**P0 类型底座已落地**（含 `item` + 各类型 `attrs` 契约 / 编辑 / 定义块摘要）。

下一优先：右栏按类型消费（列表 / 人物卡），再做关系边。不要并行大做 AI 或图谱。

---

## P0 · 类型基础

| 项 | 说明 | 状态 |
|----|------|------|
| ✅ 词条 `type` 枚举 | 普通、角色、阵营、职业、技能、地理、历史事件、装备 | 完成 |
| ✅ 默认与兼容 | 旧词条无 `type` → basic；读写 normalize | 完成 |
| ✅ 快捷键选类型 | `Ctrl+Alt+T` → 气泡；1–7 特殊 / Esc·Enter 普通 | 完成 |
| ✅ 编辑可选类型 | 右栏词条面板可改 type | 完成 |
| ✅ 类型标签 | 定义块标题旁文字标签 | 完成 |
| ✅ 特殊字段 `attrs` | 只存 glossary；按 type 解释；编辑 + 定义块摘要 | 完成 |
| 同名策略 | 全局 `title` 唯一（跨类型也不重名） | 已定 |

**落库：** 按类型写入 `data/<type>.json`（必有 `list` + `terms`）；Markdown `::: term` 不改。新字段靠 store / server `normalize`。

---

## P1 · 右栏消费

| 项 | 说明 | 依赖 |
|----|------|------|
| 按类型筛选 / 分组 | 右栏小模块：只看角色 / 技能 / 装备等 | P0 |
| 人物汇总卡片 | 聚合某「角色」的描述与引用处 | P0 + 右栏模块 |
| 历史事件总览 | 事件列表（`timeLabel` 已在 attrs） | P0 |
| 人物关系图 | **另存关系边**（from/to/relation），勿塞进 description / attrs | P0 + 边数据模型 |

右栏书签继续走 `rightPanelRegistry`；`.app-config.json` 的 `rightPanelActiveModuleId` 只记上次打开的模块。

---

## P2 · AI 文档分析（助手层，非真相源）

| 项 | 说明 | 注意 |
|----|------|------|
| 提示词文件 | 如 `prompts/doc-analyze.md`：只分析、出结构化候选、不改文件 | 可版本管理 |
| 分析 API | 入：当前文件/选区；出：JSON（类型建议、关系草稿） | AI **无写盘权限** |
| 右栏「AI 分析」 | 展示候选，用户勾选后再写入 | 与冲突抽屉同一交互哲学 |
| 密钥与配置 | 本地配置，不进仓库 | 可插拔模型 |

**AI 第一期只做一件：** 「类型建议」或「关系草稿」二选一。

---

## P3 · 可后续再议

- 各类型专用语法（`::: character` 等）——不推荐  
- `item.slotDetail`（头盔/胸甲等细部位）——有穿戴系统再加  
- 类型主题色 / 设定集 UI  
- 世界地图与地理钉点  

---

## 建议实现顺序

```
1. ✅ type + attrs 契约 + 编辑/摘要（P0）
2. 右栏：按类型列表 / 人物卡（P1 轻量）
3. 关系边模型 + 关系图（P1）
4. prompts + AI 只读分析 + 右栏确认应用（P2）
```

---

## 与现有能力的关系（备忘）

| 现有 | 后续怎么用 |
|------|------------|
| `::: term` / `term[]` / 改名 / 冲突抽屉 | 正文与词库主链路（`core/`）；`type` / `attrs` 挂 glossary |
| `core/` | 引擎：`shared` / `model` / `match` / `plugins` / `dialog` / `panel` / `rename` |
| `types/<name>/` | 每个特殊类型一目录；`registry` 聚合 |
| 右侧功能区 | 图谱、总览、AI、汇总卡的宿主 |
| `data/<type>.json` | 按类型词库：`list`（名称）+ `terms`（详情）；gitignore |
| `.app-config.json` | 应用级偏好，与词条数据分离 |
